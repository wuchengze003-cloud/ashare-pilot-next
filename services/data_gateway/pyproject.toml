[project]
name = "ashare-data-gateway"
version = "0.1.0"
description = "Provider ingestion and immutable dataset publication boundary"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
